import {useState} from 'react';
import {supabase,isSupabaseConfigured} from '../lib/supabase';
export default function Login({onLogin}:{onLogin:()=>void}){const [email,setEmail]=useState(''),[password,setPassword]=useState(''),[busy,setBusy]=useState(false),[error,setError]=useState('');const submit=async(e:any)=>{e.preventDefault();setError('');if(!isSupabaseConfigured){setError('Supabase is not configured. Open the root .env file and add your Supabase URL and Publishable Key, then restart Vite.');return;}setBusy(true);try{const {error}=await supabase.auth.signInWithPassword({email:email.trim(),password});if(error)throw error;onLogin();}catch(err:any){setError(err?.message||'Unable to sign in. Please check your connection and Supabase settings.');}finally{setBusy(false)}};return <div className="login"><div className="login-card"><div className="brand"><div className="brand-mark">E</div><div><b>EduSphere</b><small>School OS</small></div></div><h1>Welcome back</h1><p>Sign in to your school workspace.</p>{error&&<div className="alert">{error}</div>}<form onSubmit={submit}><label>Email<input type="email" value={email} onChange={e=>setEmail(e.target.value)} required/></label><label>Password<input type="password" value={password} onChange={e=>setPassword(e.target.value)} required/></label><button className="primary full" disabled={busy}>{busy?'Signing in…':'Sign in'}</button></form><small>Admin accounts are created in Supabase Authentication.</small><div className="login-contact"><b>EduSphere</b> · Developed by Hanzala Khan<br/><a href="mailto:hanzala3311211@gmail.com">hanzala3311211@gmail.com</a> · <a href="https://wa.me/923467032832" target="_blank" rel="noreferrer">WhatsApp 03467032832</a></div></div></div>}
