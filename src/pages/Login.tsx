import {useState} from 'react';
import {ArrowRight,ShieldCheck,Cloud,LockKeyhole} from 'lucide-react';
import {supabase,isSupabaseConfigured} from '../lib/supabase';
export default function Login({onLogin}:{onLogin:()=>void}){const [email,setEmail]=useState(''),[password,setPassword]=useState(''),[busy,setBusy]=useState(false),[error,setError]=useState('');const submit=async(e:any)=>{e.preventDefault();setError('');if(!isSupabaseConfigured){setError('Supabase is not configured. Open the root .env file and add your Supabase URL and Publishable Key, then restart Vite.');return;}setBusy(true);try{const {error}=await supabase.auth.signInWithPassword({email:email.trim(),password});if(error)throw error;onLogin();}catch(err:any){setError(err?.message||'Unable to sign in. Please check your connection and Supabase settings.');}finally{setBusy(false)}};return <div className="login-shell"><div className="login-visual"><div className="login-visual-inner"><div className="brand login-brand"><div className="brand-mark">E</div><div><b>EduSphere</b><small>Smart School OS</small></div></div><span className="eyebrow">PRIVATE SCHOOL WORKSPACE</span><h1>Everything your school needs,<br/><em>connected.</em></h1><p>One secure workspace for students, teachers, attendance, fees, assignments, exams and results.</p><div className="login-feature-list"><div><ShieldCheck size={17}/><span><b>Role-based access</b><small>Each account sees the right workspace.</small></span></div><div><Cloud size={17}/><span><b>Cloud-connected</b><small>Authorized changes stay synchronized.</small></span></div><div><LockKeyhole size={17}/><span><b>Private records</b><small>School information stays behind sign in.</small></span></div></div></div></div><div className="login-panel"><div className="login-panel-inner"><div className="mobile-login-brand"><div className="brand-mark">E</div><b>EduSphere</b></div><span className="eyebrow dark-eyebrow">SECURE SIGN IN</span><h1>Welcome back</h1><p>Sign in to continue to your school workspace.</p>{error&&<div className="alert">{error}</div>}<form onSubmit={submit}><label>Email<input type="email" value={email} onChange={e=>setEmail(e.target.value)} placeholder="you@school.com" required/></label><label>Password<input type="password" value={password} onChange={e=>setPassword(e.target.value)} placeholder="Enter your password" required/></label><button className="primary login-submit" disabled={busy}>{busy?'Signing in…':'Sign in'}<ArrowRight size={17}/></button></form><div className="login-rule"><span/>Authorized users only<span/></div><small className="login-help">Accounts are managed through Supabase Authentication.</small><div className="login-contact"><b>EduSphere</b> · Developed by Hanzala Khan<br/><a href="mailto:hanzala3311211@gmail.com">hanzala3311211@gmail.com</a> · <a href="https://wa.me/923467032832" target="_blank" rel="noreferrer">WhatsApp 03467032832</a></div></div></div></div>}
