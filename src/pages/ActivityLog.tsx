import {useEffect,useState} from 'react'; import {supabase} from '../lib/supabase';
export default function ActivityLog(){const [rows,setRows]=useState<any[]>([]); const [loading,setLoading]=useState(true); useEffect(()=>{supabase.from('audit_logs').select('*').order('created_at',{ascending:false}).limit(100).then(({data})=>{setRows(data||[]);setLoading(false)})},[]); return <section className="page"><div className="page-head"><div><h2>Activity Log</h2><p>Recent administrative and data activity across EduSphere.</p></div></div><div className="card"><div className="table-wrap"><table><thead><tr><th>Time</th><th>Action</th><th>Module</th><th>Record</th><th>User</th></tr></thead><tbody>{loading?<tr><td colSpan={5}>Loading activity…</td></tr>:rows.length===0?<tr><td colSpan={5}>No activity recorded yet.</td></tr>:rows.map(r=><tr key={r.id}><td>{new Date(r.created_at).toLocaleString()}</td><td><b>{r.action}</b></td><td>{r.table_name}</td><td>{r.record_id||'—'}</td><td>{r.actor_id||'System'}</td></tr>)}</tbody></table></div></div></section>}
