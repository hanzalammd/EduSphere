import {useEffect,useState} from 'react';
import {Bell,Menu} from 'lucide-react';
import {getProfile} from '../lib/api';
const subtitles:Record<string,string>={Dashboard:'School overview and today’s activity',Students:'Student records and academic details',Teachers:'Faculty records and teaching information',Classes:'Classes, timetables and exam schedules','Student Attendance':'Daily student attendance','Teacher Attendance':'Staff attendance records',Fees:'Fees, balances and payment history',Assignments:'Academic assignments','Assignment Submissions':'Submission records and progress',Exams:'Examinations and academic terms',Results:'Academic results', 'Fee Payments':'Payment records',Notices:'School announcements',Reports:'Operational summaries',Settings:'School configuration',About:'About EduSphere','Activity Log':'System activity'};
export default function Header({title,onMenu}:{title:string;onMenu?:()=>void}){const [name,setName]=useState('Administrator'),[role,setRole]=useState('');useEffect(()=>{getProfile().then(p=>{if(p?.full_name)setName(p.full_name);if(p?.role)setRole(p.role)}).catch(()=>{})},[]);return <header className="app-header"><button className="mobile-menu-button" onClick={onMenu} aria-label="Open navigation"><Menu size={20}/></button><div className="header-title"><div className="header-breadcrumb">EduSphere <span>/</span></div><div className="header-page-title"><b>{title}</b><small>{subtitles[title]||'Manage your school workspace'}</small></div></div><div className="header-right"><button className="header-icon" aria-label="Notifications"><Bell size={18}/><i/></button><div className="user-pill"><span className="avatar">{name[0]?.toUpperCase()}</span><span><b>{name}</b><small>{role||'user'}</small></span></div></div></header>}
