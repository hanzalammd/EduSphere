import {useEffect,useState} from 'react'; import {getProfile} from '../lib/api'; export default function Header({title}:{title:string}){const [name,setName]=useState('Administrator');useEffect(()=>{getProfile().then(p=>p?.full_name&&setName(p.full_name)).catch(()=>{})},[]);return <header><div><span className="crumb">EduSphere / </span><b>{title}</b></div><div className="user-pill"><span className="avatar">{name[0]}</span><span>{name}</span></div></header>}
